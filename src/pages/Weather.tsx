import WeatherSearch from "../features/weather/WeatherSearch";

const Weather = () => {
  return (
    <section>
      <h2>Weather App</h2>
      <WeatherSearch />
    </section>
  );
};

export default Weather;